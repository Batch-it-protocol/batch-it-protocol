[package]
name = "spike_pda_buyer_lite"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Minimal non-Anchor spike: PDA buy CPI into pump (small enough for low-SOL deploy)"

[lib]
crate-type = ["cdylib", "lib"]
name = "spike_pda_buyer_lite"

[dependencies]
solana-program = "2.2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
